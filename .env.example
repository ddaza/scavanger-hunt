# Server
PORT=8080

# Twilio (optional for future signature validation)
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Skip authentication check
TWILIO_SKIP_VERIFY=false

# Optional: public base URL used for signature validation behind proxies.
# Example: https://scavenger-hunt.onrender.com
# If not set, X-Forwarded-* headers are used.
PUBLIC_BASE_URL=
